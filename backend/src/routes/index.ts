// Importe la fonction login depuis le module login
import { login } from "./login";
// Importe la fonction register depuis le module register
import { register } from "./register";

// Exporte les fonctions login et register pour les utiliser dans d'autres parties de l'application
export { login, register };
